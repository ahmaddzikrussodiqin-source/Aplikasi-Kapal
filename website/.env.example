# API Configuration
# =============================================================================

# App Mode: 'development' (localhost) or 'production' (Railway)
VITE_APP_MODE=production

# For local development (when VITE_APP_MODE=development)
VITE_API_URL=http://localhost:3000

# Railway URL for production - MUST match Android app Config.BASE_URL
# Android app uses: https://aplikasi-kapal-production.up.railway.app
VITE_RAILWAY_URL=https://aplikasi-kapal-production.up.railway.app

# =============================================================================
# PANDUAN (GUIDE):
# =============================================================================
#
# Untuk Testing dengan Localhost:
# 1. Ubah VITE_APP_MODE menjadi 'development'
# 2. Pastikan backend running di localhost:3000
# 3. Jalankan: npm run dev
#
# Untuk Production (Railway):
# 1. VITE_APP_MODE='production' (default)
# 2. File URLs akan menggunakan Railway URL
#
# CATATAN: Untuk file uploads, selalu gunakan Railway URL agar
#         file bisa diakses dari Android app juga
# =============================================================================

